# macOS
.DS_Store

# Secrets (real keys live here; never commit — TEAM_PLAN §1.5)
Secrets.swift

# Xcode - build & derived data
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Xcode - per-user state (should never be shared)
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (if ever used)
Pods/

# Carthage (if ever used)
Carthage/Build/

# Accio (if ever used)
Dependencies/
.accio/

# fastlane (if ever used)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
